{
  "private": true,
  "devDependencies": {
    "@unumux/ux-build-tools": "^3.6.0",
    "imagemin-cli": "^3.0.0"
  },
  "scripts": {
    "minify": "imagemin images-src/* --out-dir=images",
    "start": "gulp --gulpfile=node_modules/@unumux/ux-build-tools/index.js --cwd=."
  },
  "dependencies": {
    "scrollspy-js": "^0.1.3"
  }
}
